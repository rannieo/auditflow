# Claude Code / Claude-related files
CLAUDE.md
.claude/
.claude.json
.claude.local.json

# Anthropic Claude Code memory + project state (typically lives outside the
# repo, but listed here defensively in case anything ends up checked in)
.claude/memory/
.claude/settings.json
.claude/settings.local.json
.claude/projects/

# Plan / docs / scratchpads generated during Claude sessions
docs/*

# OS / editor noise that tends to creep in alongside agent work
.DS_Store

web/coverage